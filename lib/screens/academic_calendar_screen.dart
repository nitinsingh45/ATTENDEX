// screens/academic_calendar_screen.dart
import 'package:flutter/material.dart';

class AcademicCalendarScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Academic Calendar'),
      ),
      body: Center(
        child: Text('Academic Calendar Screen'),
      ),
    );
  }
}
