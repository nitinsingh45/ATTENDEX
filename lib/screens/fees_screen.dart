// screens/fees_screen.dart
import 'package:flutter/material.dart';

class FeesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fees'),
      ),
      body: Center(
        child: Text('Fees Screen'),
      ),
    );
  }
}
