// screens/transport_screen.dart
import 'package:flutter/material.dart';

class TransportScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Transport'),
      ),
      body: Center(
        child: Text('Transport Screen'),
      ),
    );
  }
}
